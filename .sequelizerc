const { resolve } = require('path'); //Usado para usarmos o caminho relativo do nosso projetos

module.exports = { //Aqui nesse linha de codigo estamos a configurar os caminhos das coisas necessarias
  config: resolve(__dirname, 'src', 'config', 'database.js'), //caminho do nosso arquivo de configuração;
  'models-path': resolve(__dirname, 'src', 'models'),//caminho dos nosso models
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), //caminho das nossas migrations(são as nossas alterações que são feitas na nossa base de dados);
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),//
};
